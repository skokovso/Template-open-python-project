# =====================================================================
# Универсальный .gitignore — проекты: llm_answer, DictoOn, MyStudent
# Правило: в репозиторий не попадают персональные данные, большие модели,
# кэши, секреты и runtime-файлы. Код, схемы, шаблоны и документация — попадают.
# =====================================================================

# ---------------------------------------------------------------------
# 1. Большие модели (ASR / пост-коррекция)
#    Исключаются только gigaam и t5. Vosk НЕ исключается (лёгкий, коммитится).
# ---------------------------------------------------------------------
models/gigaam/
models/t5/
models/Models.zip

# ---------------------------------------------------------------------
# 2. Персональные данные и пользовательский контент
#    Сюда складываем всё с ФИО, историями болезней, рабочими базами.
# ---------------------------------------------------------------------
personal/
personal/data/
data/
data_archive/
archive/
enter_data/
restore_my/
reports/
SonologData/

# ---------------------------------------------------------------------
# 3. Большие файлы проекта (не модели)
# ---------------------------------------------------------------------
big_files/

# ---------------------------------------------------------------------
# 4. Резервные копии
# ---------------------------------------------------------------------
_backup/

# ---------------------------------------------------------------------
# 5. Кэши, временные файлы, сборки
# ---------------------------------------------------------------------
cache/
hub/
dist/
build/
__pycache__/
*.pyc
*.pyo
*.spec

# ---------------------------------------------------------------------
# 6. Секреты и локальные настройки
#    .env.example коммитится как образец заполнения.
# ---------------------------------------------------------------------
.env
.env.*
!.env.example
config.local.json
settings.local.ini

# ---------------------------------------------------------------------
# 7. Excel и базы данных (почти всегда содержат персональные данные)
#    Шаблоны без данных, которые нужно коммитить, кладите в docs/templates/.
# ---------------------------------------------------------------------
*.xlsx
*.xls
!docs/templates/*.xlsx
import_analysis.xlsx
*.db
*.sqlite
*.sqlite3
*-journal

# ---------------------------------------------------------------------
# 8. Логи
# ---------------------------------------------------------------------
*.log
logs/

# ---------------------------------------------------------------------
# 9. Мусор ОС и редакторов
# ---------------------------------------------------------------------
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/

# ---------------------------------------------------------------------
# 10. MyStudent: runtime-данные и кэши приложения
#     (uploads/ — вложения занятий, qr_static/ — кэш QR на локальной машине)
# ---------------------------------------------------------------------
uploads/
qr_static/